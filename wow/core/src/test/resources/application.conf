akka {
  loglevel = "DEBUG"
  scheduler {
    tick-duration = 10ms
  }
}

wow-test {
  test {
      connection = "jdbc:postgresql://localhost:5432/ensiwow?currentSchema=test"
      username = "ensiwow"
      password = ""
  }
}

